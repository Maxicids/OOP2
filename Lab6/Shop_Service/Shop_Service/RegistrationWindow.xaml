<Window x:Class="Shop_Service.RegistrationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:Shop_Service"
        mc:Ignorable="d"
        Title="RegistrationWindow" Height="450" Width="800" WindowStyle="None" ResizeMode="CanResize" Icon="MainIcon.png">
    <Grid Name="RegisterGrid" Background="#FFDCEEEE" MouseDown="RegistrationWindow_OnMouseDown">
        <StackPanel>
            <Grid Background="#a6e6fc" HorizontalAlignment="Left" Height="20" VerticalAlignment="Top" Width="{Binding ElementName=RegisterGrid, Path=ActualWidth}">
                <Button Name="CloseButton" Padding="0" HorizontalAlignment="Right" Margin="0, -5, 0, 0"
                        Background="{x:Null}" BorderBrush="{x:Null}" Click="ButtonClose_OnClick" Width="20"> 
                    <materialDesign:PackIcon Kind="Close" Foreground="Gray"/>
                </Button>
            </Grid>
        </StackPanel>

        <Border Canvas.ZIndex="1" Background="#FFDCEEEE" Width="80" Height="80" CornerRadius="80" Margin="596,0,0,0">
            <Border Cursor="Hand" MouseDown="Register" Background="#a6e6fc" Width="60" Height="60" CornerRadius="60">
                <Polygon Canvas.ZIndex="1" Fill="#FFFF9CAB" Points="25,20,25,40,40,30" />
            </Border>
        </Border>
        <Rectangle Fill="White" MinWidth="400" MaxWidth="600" VerticalAlignment="Center" Height="210" RadiusX="15" RadiusY="15">
            <Rectangle.Effect>
                <DropShadowEffect Color="LightGray" ShadowDepth="2"></DropShadowEffect>
            </Rectangle.Effect>
        </Rectangle>
        <TextBox Background="Transparent" Text="Registration" FontSize="50" FontFamily="Calibri Bold" TextAlignment="Center" 
                 Width="300" Margin="246,35,246,333" RenderTransformOrigin="0.487,-0.41"/>
        <TextBox Name="TbLogin" Width="500" Height="40" VerticalAlignment="Top" materialDesign:HintAssist.Hint="Username" 
                 Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0 145 0 0"></TextBox>
        <PasswordBox Name="TbPassword" Width="500" Height="40" VerticalAlignment="Top" materialDesign:HintAssist.Hint="Password"
                     Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" Margin="0 190 0 0"/>
        <PasswordBox Name="TbRepeatPassword" Width="500" Height="40" VerticalAlignment="Top" materialDesign:HintAssist.Hint="Repeat Password"
                     Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" Margin="0 235 0 0"/>
        <StackPanel Cursor="Hand" MouseDown="ToLogin" Background="Transparent"  Width="240" Height="30" Margin="0, 150, 0, 0"  Canvas.ZIndex="1">

        </StackPanel> 
        <TextBox  BorderBrush="{x:Null}" Background="Transparent" Text="already have an account? click here" FontSize="10" 
                 FontFamily="Calibri Bold" TextAlignment="Center" Margin="286.667,290,289.333,135" RenderTransformOrigin="0.487,-0.41"/>
    </Grid>
</Window>
